apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{.Release.Name}}-delete-task-api-ingress
spec:
  ingressClassName: {{.Values.common.ingressClassName}}
  rules:
  - host: {{.Release.Name}}.delete-task-api.{{.Values.common.hostDomain}}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{.Release.Name}}-todo-delete-tasks-api-svc
            port: 
              number: {{.Values.deleteTask.svcPort}}
